name: Qinster v235 training result stat
on:
  push:
    paths:
      - 'scripts/patch-v235-training-result-stat.py'
      - '.github/workflows/v235-training-result-stat.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply v235 patch
        run: python scripts/patch-v235-training-result-stat.py
      - name: Checks
        run: |
          node --check v199-roguelike-expedition.js
          node --check game.js
          node --check v201-battle-theater.js
          npm test
      - name: Commit patch
        run: |
          git config user.name 'Qinster Patch Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add v199-roguelike-expedition.js game.js index.html
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Show actual post-training stat and bump v235'
            git push
          fi
